allprojects {
    buildscript {
        repositories {
            google()
        }
    }
}

// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

apply plugin: 'com.android.library'


dependencies {
    implementation 'com.google.ar:core:1.22.0'
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation(name: 'VuforiaEngine', ext: 'aar')
}

android {
    compileSdkVersion 30
    buildToolsVersion '28.0.3'

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    defaultConfig {
        minSdkVersion 24
        targetSdkVersion 30
        ndk {
            abiFilters 'armeabi-v7a'
        }
        versionCode 1
        versionName '0.0.1'
        consumerProguardFiles 'proguard-unity.txt'
    }

    lintOptions {
        abortOnError false
    }

    aaptOptions {
        ignoreAssetsPattern = "!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~"
    }

    packagingOptions {
        doNotStrip '*/armeabi-v7a/*.so'
    }

    android.libraryVariants.all { variant ->
        variant.outputs.all {
            def fileName = "AirLookSuperApp.aar"
            outputFileName = fileName
        }
    }
}
apply plugin: 'maven'
uploadArchives {
    repositories.mavenDeployer {
        def mavenDirPath = file('D:\\mavendir') // 本地存放目录(自行选择)，可放在gradle.properties文件中引用
        repository(url: "file://${mavenDirPath.absolutePath}") // 必须双引号，单引号不会转义$
        pom.project {
            groupId "com.airlook.player" // 可以随意取，一般取包名
            artifactId "unityPlayer" // 可以随意取，一般取库的名字
            version "1.0.1" // 版本号
        }
    }
}

